import React from "react"

// const pokemon = ["Bulbasaur", "Charmander", "Squirtle"]

// const PTag = pokemon.map(poke => `<p>${poke}</p>`)

// console.log(PTag)

export default function Function() {
    const colors = [ 
        "Red", 
    "Green", 
    "Blue", 
    "Orange", 
    "Indigo", 
    "Purple"
]
    
    return (
    <div>
    {colors}
    </div>
    )
}